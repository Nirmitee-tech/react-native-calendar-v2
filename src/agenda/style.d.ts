import { StyleSheet } from 'react-native';
import { Theme } from '../types';
/**
 * Creates styles for the Agenda component
 * @param theme - Optional theme object to override default styles
 * @returns StyleSheet object for the Agenda component
 */
export default function styleConstructor(theme?: Partial<Theme>): StyleSheet.NamedStyles<any>;
